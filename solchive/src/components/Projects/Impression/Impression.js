import React, { Component } from 'react';

class Impression extends Component {
    render() {
        const { name, imoji, text, bg_color } = this.props;
        return (
            <div>
                
            </div>
        );
    }
}

export default Impression;